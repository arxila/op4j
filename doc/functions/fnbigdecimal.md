# Functions: FnBigDecimal

## 1. Overview

[FnBigDecimal](http://www.op4j.org/apidocs/org/op4j/functions/FnBigDecimal.html) is a *function hub class* containing many static methods to work with BigDecimal objects. These static methods return functions that take a **BigDecimal** object either as input or output (depending on the function). It includes conversions to String, currency representations, and logical operations between BigDecimal objects, among others.

---

## 2. Functions

| Function name      | Type                                   | Params                                                                 | Description |
|--------------------|----------------------------------------|------------------------------------------------------------------------|-------------|
| **abs**            | Function<BigDecimal, BigDecimal>        |                                                                        | Returns the absolute value of the input |
| **add**            | Function<BigDecimal, BigDecimal>        | Number add<br>byte add<br>short add<br>int add<br>long add<br>float add<br>double add | Adds the given number to the target and returns the result |
| **avg**            | Function<Iterable<BigDecimal>, BigDecimal> | <br>MathContext mathContext<br>RoundingMode roundingMode              | Returns the average of all numbers in the Iterable input object. MathContext or RoundingMode can be used for rounding and precision |
| **avgArray**       | Function<BigDecimal[], BigDecimal>      | <br>MathContext mathContext<br>RoundingMode roundingMode              | Returns the average of all numbers in the input array. MathContext or RoundingMode can be used for rounding and precision |
| **between**        | Function<BigDecimal,Boolean>            | Number min<br>Number max                                               | Determines if the target is between min and max |
| **divideBy**       | Function<BigDecimal, BigDecimal>        | Number divisor<br>byte divisor<br>short divisor<br>int divisor<br>long divisor<br>float divisor<br>double divisor<br>Number divisor + MathContext<br>byte divisor + MathContext<br>short divisor + MathContext<br>int divisor + MathContext<br>long divisor + MathContext<br>float divisor + MathContext<br>double divisor + MathContext<br>Number divisor + RoundingMode<br>byte divisor + RoundingMode<br>short divisor + RoundingMode<br>int divisor + RoundingMode<br>long divisor + RoundingMode<br>float divisor + RoundingMode<br>double divisor + RoundingMode | Divides the target by the given divisor and returns the result. MathContext or RoundingMode can be used for rounding and precision |
| **eq**             | Function<BigDecimal,Boolean>            | BigDecimal object                                                      | Checks if the target and specified object are equal (equals method) |
| **eqBy**           | Function<BigDecimal,Boolean>            | IFunction<BigDecimal,?> by<br>Object object                           | Checks if the result of executing the function on the target and object are equal |
| **eqValue**        | Function<BigDecimal,Boolean>            | BigDecimal object                                                      | Checks if the target and specified object are equal in value (compareTo == 0) |
| **eqValueBy**      | Function<BigDecimal,Boolean>            | IFunction<BigDecimal,?> by<br>Object object                           | Checks if the result of executing the function on the target and object are equal in value |
| **greaterOrEqTo**  | Function<BigDecimal,Boolean>            | Number object                                                          | Checks if the target is greater or equal to the specified object |
| **greaterOrEqToBy**| Function<BigDecimal,Boolean>            | IFunction<BigDecimal,?> by<br>Object object                           | Checks if the result of executing the function on the target is greater or equal to the object |
| **greaterThan**    | Function<BigDecimal,Boolean>            | Number object                                                          | Checks if the target is greater than the specified object |
| **greaterThanBy**  | Function<BigDecimal,Boolean>            | IFunction<BigDecimal,?> by<br>Object object                           | Checks if the result of executing the function on the target is greater than the object |
| **isNotNull**      | Function<BigDecimal,Boolean>            |                                                                        | Checks if the target is not null |
| **isNull**         | Function<BigDecimal,Boolean>            |                                                                        | Checks if the target is null |
| **lessOrEqTo**     | Function<BigDecimal,Boolean>            | Number object                                                          | Checks if the target is less or equal to the specified object |
| **lessOrEqToBy**   | Function<BigDecimal,Boolean>            | IFunction<BigDecimal,?> by<br>Object object                           | Checks if the result of executing the function on the target is less or equal to the object |
| **lessThan**       | Function<BigDecimal,Boolean>            | Number object                                                          | Checks if the target is less than the specified object |
| **lessThanBy**     | Function<BigDecimal,Boolean>            | IFunction<BigDecimal,?> by<br>Object object                           | Checks if the result of executing the function on the target is less than the object |
| **max**            | Function<Iterable<BigDecimal>, BigDecimal> |                                                                        | Returns the maximum number from the Iterable input object |
| **maxArray**       | Function<BigDecimal[], BigDecimal>      |                                                                        | Returns the maximum number from the input array |
| **min**            | Function<Iterable<BigDecimal>, BigDecimal> |                                                                        | Returns the minimum number from the Iterable input object |
| **minArray**       | Function<BigDecimal[], BigDecimal>      |                                                                        | Returns the minimum number from the input array |
| **multiplyBy**     | Function<BigDecimal, BigDecimal>        | Number multiplicand<br>byte multiplicand<br>short multiplicand<br>int multiplicand<br>long multiplicand<br>float multiplicand<br>double multiplicand<br>Number multiplicand + MathContext<br>byte multiplicand + MathContext<br>short multiplicand + MathContext<br>int multiplicand + MathContext<br>long multiplicand + MathContext<br>float multiplicand + MathContext<br>double multiplicand + MathContext<br>Number multiplicand + RoundingMode<br>byte multiplicand + RoundingMode<br>short multiplicand + RoundingMode<br>int multiplicand + RoundingMode<br>long multiplicand + RoundingMode<br>float multiplicand + RoundingMode<br>double multiplicand + RoundingMode | Multiplies the target by the given multiplicand and returns the result. MathContext or RoundingMode can be used for rounding and precision |
| **notEq**          | Function<BigDecimal,Boolean>            | BigDecimal object                                                      | Checks if the target and specified object are NOT equal |
| **notEqBy**        | Function<BigDecimal,Boolean>            | IFunction<BigDecimal,?> by<br>Object object                           | Checks if the result of executing the function on the target and object are NOT equal |
| **notEqValue**     | Function<BigDecimal,Boolean>            | BigDecimal object                                                      | Checks if the target and specified object are NOT equal in value |
| **notEqValueBy**   | Function<BigDecimal,Boolean>            | IFunction<BigDecimal,?> by<br>Object object                           | Checks if the result of executing the function on the target and object are NOT equal in value |
| **pow**            | Function<BigDecimal, BigDecimal>        | int power<br>int power + MathContext<br>int power + RoundingMode            | Raises the target to the given power and returns the result. MathContext or RoundingMode can be used for rounding and precision |
| **remainder**      | Function<BigDecimal, BigDecimal>        | Number divisor<br>byte divisor<br>short divisor<br>int divisor<br>long divisor<br>float divisor<br>double divisor<br>Number divisor + MathContext<br>byte divisor + MathContext<br>short divisor + MathContext<br>int divisor + MathContext<br>long divisor + MathContext<br>float divisor + MathContext<br>double divisor + MathContext | Divides the target by the given divisor and returns the remainder (target % divisor). MathContext can be used for rounding and precision |
| **round**          | Function<BigDecimal, BigDecimal>        | MathContext mathContext<br>RoundingMode roundingMode              | Rounds the input number with the given MathContext or RoundingMode |
| **roundBigDecimal**| Function<BigDecimal,BigDecimal>         | int scale<br>RoundingMode roundingMode                              | Rounds the input number with the given scale and RoundingMode |
| **subtract**       | Function<BigDecimal, BigDecimal>        | Number subtract<br>byte subtract<br>short subtract<br>int subtract<br>long subtract<br>float subtract<br>double subtract | Subtracts the given number from the target and returns the result |
| **sum**            | Function<Iterable<BigDecimal>, BigDecimal> |                                                                        | Returns the sum of all numbers in the Iterable input object |
| **sumArray**       | Function<BigDecimal[], BigDecimal>      |                                                                        | Returns the sum of all numbers in the input array |
| **toCurrencyStr**  | Function<BigDecimal,String>             | <br>boolean groupingUsed<br>Locale locale<br>String locale<br>Locale locale + boolean groupingUsed<br>String locale + boolean groupingUsed<br>Locale locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed<br>String locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed<br>Locale locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + char groupingSeparator + char decimalSeparator + boolean decimalSeparatorAlwaysShown<br>Locale locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed + char decimalSeparator + boolean decimalSeparatorAlwaysShown<br>String locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + char groupingSeparator + char decimalSeparator + boolean decimalSeparatorAlwaysShown<br>String locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed + char decimalSeparator + boolean decimalSeparatorAlwaysShown | Returns the String representation of the target as a currency in the specified locale, with options for formatting |
| **toPercentStr**   | Function<BigDecimal,String>             | <br>boolean groupingUsed<br>Locale locale<br>String locale<br>Locale locale + boolean groupingUsed<br>String locale + boolean groupingUsed<br>Locale locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed<br>String locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed<br>Locale locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + char groupingSeparator + char decimalSeparator + boolean decimalSeparatorAlwaysShown<br>Locale locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed + char decimalSeparator + boolean decimalSeparatorAlwaysShown<br>String locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + char groupingSeparator + char decimalSeparator + boolean decimalSeparatorAlwaysShown<br>String locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed + char decimalSeparator + boolean decimalSeparatorAlwaysShown | Returns a String representing a percentage from the target number, with options for formatting |
| **toStr**          | Function<BigDecimal,String>             | <br>boolean groupingUsed<br>Locale locale<br>String locale<br>Locale locale + boolean groupingUsed<br>String locale + boolean groupingUsed<br>Locale locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed<br>String locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed<br>Locale locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + char groupingSeparator + char decimalSeparator + boolean decimalSeparatorAlwaysShown<br>Locale locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed + char decimalSeparator + boolean decimalSeparatorAlwaysShown<br>String locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + char groupingSeparator + char decimalSeparator + boolean decimalSeparatorAlwaysShown<br>String locale + int minIntegerDigits + int minFractionDigits + int maxFractionDigits + boolean groupingUsed + char decimalSeparator + boolean decimalSeparatorAlwaysShown | Returns the String representation of the input number, with options for formatting |
